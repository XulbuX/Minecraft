import type * as Interfaces from '@/interfaces';

declare global {
  export interface MinecraftColor extends Interfaces.MinecraftColor { }
  export interface SignType extends Interfaces.SignType { }
  export interface MinecraftColors extends Interfaces.MinecraftColors { }
  export interface SignTypes extends Interfaces.SignTypes { }
}
